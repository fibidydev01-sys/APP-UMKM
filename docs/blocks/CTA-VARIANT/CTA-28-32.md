# CTA COMPONENTS 28-32 WITH REACT BITS

5 CTA (Call-to-Action) variants dengan advanced React Bits combinations - **NO
BACKGROUND EFFECTS**, fokus pada creative text animations dan premium
interactions!

---

## CTA-28: ASCII Text with Circular Text Decoration

**Layout**: Center dengan ASCII art title  
**Animation**: ASCIIText, CircularText untuk decoration **Style**: Retro tech
aesthetic

```tsx
// Cta28.tsx
'use client';

import Link from 'next/link';
import { ArrowRight } from 'lucide-react';
import { Button } from '@/components/ui/button';
import ASCIIText from '@/components/ui/ASCIIText';
import CircularText from '@/components/ui/CircularText';
import ClickSpark from '@/components/ui/ClickSpark';

/**
 * Cta28 Props - Mapped from Data Contract (LANDING-DATA-CONTRACT.md)
 *
 * @prop title - ctaTitle: CTA heading
 * @prop subtitle - ctaSubtitle: CTA description
 * @prop buttonText - ctaButtonText: Button label
 * @prop buttonLink - ctaButtonLink: Button destination URL
 * @prop buttonVariant - ctaButtonStyle: 'default' | 'secondary' | 'outline'
 */
interface Cta28Props {
  title: string;
  subtitle?: string;
  buttonText: string;
  buttonLink: string;
  buttonVariant: 'default' | 'secondary' | 'outline';
}

/**
 * CTA Block: cta28
 * Design: ASCII Text with Circular Text Decoration
 */
export function Cta28({
  title,
  subtitle,
  buttonText,
  buttonLink,
  buttonVariant,
}: Cta28Props) {
  return (
    <section className="relative py-20 my-8">
      {/* Circular Text Background Decoration */}
      <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-96 h-96 opacity-5 pointer-events-none">
        <CircularText
          text="✦ GET STARTED ✦ JOIN NOW ✦ "
          onHover="speedUp"
          spinDuration={20}
        />
      </div>

      <div className="text-center max-w-4xl mx-auto px-4 relative z-10">
        {/* ASCII Art Title */}
        <div className="mb-6">
          <ASCIIText
            text={title}
            font="Standard"
            colored
            gradient={['hsl(var(--primary))', 'hsl(var(--primary) / 0.6)']}
            animateOn="view"
            speed={30}
          />
        </div>

        {subtitle && (
          <p className="text-lg md:text-xl text-muted-foreground mb-8 max-w-2xl mx-auto">
            {subtitle}
          </p>
        )}

        <ClickSpark
          sparkColor="hsl(var(--primary))"
          sparkSize={12}
          sparkRadius={20}
          sparkCount={10}
          duration={400}
        >
          <Link href={buttonLink}>
            <Button
              size="lg"
              variant={buttonVariant}
              className="gap-2 h-14 px-8 text-lg"
            >
              {buttonText}
              <ArrowRight className="h-5 w-5" />
            </Button>
          </Link>
        </ClickSpark>
      </div>
    </section>
  );
}
```

---

## CTA-29: Curved Loop Text with Profile Cards

**Layout**: Center dengan curved loop text dan card display  
**Animation**: CurvedLoop, ProfileCard untuk team/testimonials **Style**: Modern
dengan flowing text

```tsx
// Cta29.tsx
'use client';

import Link from 'next/link';
import { ArrowRight } from 'lucide-react';
import { Button } from '@/components/ui/button';
import CurvedLoop from '@/components/ui/CurvedLoop';
import ShinyText from '@/components/ui/ShinyText';
import Magnet from '@/components/ui/Magnet';

/**
 * Cta29 Props - Mapped from Data Contract (LANDING-DATA-CONTRACT.md)
 *
 * @prop title - ctaTitle: CTA heading
 * @prop subtitle - ctaSubtitle: CTA description
 * @prop buttonText - ctaButtonText: Button label
 * @prop buttonLink - ctaButtonLink: Button destination URL
 * @prop buttonVariant - ctaButtonStyle: 'default' | 'secondary' | 'outline'
 */
interface Cta29Props {
  title: string;
  subtitle?: string;
  buttonText: string;
  buttonLink: string;
  buttonVariant: 'default' | 'secondary' | 'outline';
}

/**
 * CTA Block: cta29
 * Design: Curved Loop Text Background
 */
export function Cta29({
  title,
  subtitle,
  buttonText,
  buttonLink,
  buttonVariant,
}: Cta29Props) {
  return (
    <section className="relative py-20 my-8 border-t border-b border-border overflow-hidden">
      {/* Curved Loop Background */}
      <div className="absolute inset-0 opacity-10 pointer-events-none">
        <CurvedLoop
          marqueeText="✦ START TODAY ✦ JOIN US ✦ "
          speed={2}
          curveAmount={400}
          direction="right"
          interactive={false}
        />
      </div>

      <div className="text-center max-w-4xl mx-auto px-4 relative z-10">
        {/* Shiny Text Title */}
        <ShinyText
          text={title}
          speed={2}
          color="hsl(var(--foreground))"
          shineColor="hsl(var(--primary))"
          spread={120}
          direction="left"
          className="text-4xl md:text-5xl lg:text-6xl font-extrabold mb-6"
        />

        {subtitle && (
          <p className="text-lg md:text-xl text-muted-foreground mb-8 max-w-2xl mx-auto">
            {subtitle}
          </p>
        )}

        <Magnet padding={60} magnetStrength={60}>
          <Link href={buttonLink}>
            <Button
              size="lg"
              variant={buttonVariant}
              className="gap-2 h-14 px-8 text-lg shadow-lg"
            >
              {buttonText}
              <ArrowRight className="h-5 w-5" />
            </Button>
          </Link>
        </Magnet>
      </div>
    </section>
  );
}
```

---

## CTA-30: Animated List with Gradient Border

**Layout**: Split dengan animated scrolling list  
**Animation**: AnimatedList, GradientText **Style**: Dynamic list showcase

```tsx
// Cta30.tsx
'use client';

import { useState } from 'react';
import Link from 'next/link';
import { ArrowRight, Check } from 'lucide-react';
import { Button } from '@/components/ui/button';
import AnimatedList from '@/components/ui/AnimatedList';
import GradientText from '@/components/ui/GradientText';

/**
 * Cta30 Props - Mapped from Data Contract (LANDING-DATA-CONTRACT.md)
 *
 * @prop title - ctaTitle: CTA heading
 * @prop subtitle - ctaSubtitle: CTA description
 * @prop buttonText - ctaButtonText: Button label
 * @prop buttonLink - ctaButtonLink: Button destination URL
 * @prop buttonVariant - ctaButtonStyle: 'default' | 'secondary' | 'outline'
 */
interface Cta30Props {
  title: string;
  subtitle?: string;
  buttonText: string;
  buttonLink: string;
  buttonVariant: 'default' | 'secondary' | 'outline';
}

/**
 * CTA Block: cta30
 * Design: Animated List Features
 */
export function Cta30({
  title,
  subtitle,
  buttonText,
  buttonLink,
  buttonVariant,
}: Cta30Props) {
  const features = [
    'Unlimited Projects',
    'Advanced Analytics',
    'Priority Support',
    'Custom Integrations',
    'Team Collaboration',
    'Automated Workflows',
    'Security & Compliance',
    'API Access',
  ];

  return (
    <section className="py-20 my-8">
      <div className="container mx-auto px-4">
        <div className="max-w-5xl mx-auto rounded-3xl bg-card border border-border p-8 md:p-12">
          <div className="grid md:grid-cols-2 gap-8 items-center">
            {/* Left: Text Content */}
            <div>
              <GradientText
                colors={[
                  'hsl(var(--primary))',
                  'hsl(var(--primary) / 0.6)',
                  'hsl(var(--primary))',
                ]}
                animationSpeed={8}
                showBorder={false}
                className="text-4xl md:text-5xl font-bold mb-4"
              >
                {title}
              </GradientText>

              {subtitle && (
                <p className="text-lg text-muted-foreground mb-6">{subtitle}</p>
              )}

              <Link href={buttonLink}>
                <Button
                  size="lg"
                  variant={buttonVariant}
                  className="gap-2 h-14 px-8 text-lg"
                >
                  {buttonText}
                  <ArrowRight className="h-5 w-5" />
                </Button>
              </Link>
            </div>

            {/* Right: Animated List */}
            <div className="h-[400px] rounded-2xl border border-border overflow-hidden">
              <AnimatedList
                items={features}
                onItemSelect={(item, index) => console.log(item, index)}
                showGradients
                enableArrowNavigation
                displayScrollbar
              />
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
```

---

## CTA-31: Stepper Progress with Text Pressure

**Layout**: Multi-step showcase dengan variable font  
**Animation**: Stepper, TextPressure (if available) **Style**: Interactive
progression

```tsx
// Cta31.tsx
'use client';

import { useState } from 'react';
import Link from 'next/link';
import { ArrowRight } from 'lucide-react';
import { Button } from '@/components/ui/button';
import Stepper, { Step } from '@/components/ui/Stepper';
import SplitText from '@/components/ui/SplitText';
import ElectricBorder from '@/components/ui/ElectricBorder';

/**
 * Cta31 Props - Mapped from Data Contract (LANDING-DATA-CONTRACT.md)
 *
 * @prop title - ctaTitle: CTA heading
 * @prop subtitle - ctaSubtitle: CTA description
 * @prop buttonText - ctaButtonText: Button label
 * @prop buttonLink - ctaButtonLink: Button destination URL
 * @prop buttonVariant - ctaButtonStyle: 'default' | 'secondary' | 'outline'
 */
interface Cta31Props {
  title: string;
  subtitle?: string;
  buttonText: string;
  buttonLink: string;
  buttonVariant: 'default' | 'secondary' | 'outline';
}

/**
 * CTA Block: cta31
 * Design: Interactive Stepper Journey
 */
export function Cta31({
  title,
  subtitle,
  buttonText,
  buttonLink,
  buttonVariant,
}: Cta31Props) {
  const [currentStep, setCurrentStep] = useState(1);

  return (
    <section className="py-20 my-8">
      <div className="max-w-4xl mx-auto px-4">
        <div className="text-center mb-12">
          <SplitText
            text={title}
            className="text-4xl md:text-5xl lg:text-6xl font-extrabold mb-4"
            delay={50}
            duration={1.25}
            ease="power3.out"
            splitType="chars"
            from={{ opacity: 0, y: 40 }}
            to={{ opacity: 1, y: 0 }}
            threshold={0.1}
            textAlign="center"
          />

          {subtitle && (
            <p className="text-lg text-muted-foreground">{subtitle}</p>
          )}
        </div>

        <ElectricBorder
          borderColor="hsl(var(--primary))"
          borderWidth={2}
          glowIntensity={0.5}
          animationSpeed={2}
          className="rounded-3xl overflow-hidden"
        >
          <div className="p-8 bg-card">
            <Stepper
              initialStep={1}
              onStepChange={(step) => setCurrentStep(step)}
              onFinalStepCompleted={() => console.log('Journey complete!')}
              backButtonText="Back"
              nextButtonText="Next"
            >
              <Step>
                <div className="text-center py-8">
                  <h3 className="text-2xl font-bold mb-4">Step 1: Sign Up</h3>
                  <p className="text-muted-foreground">
                    Create your account in seconds
                  </p>
                </div>
              </Step>

              <Step>
                <div className="text-center py-8">
                  <h3 className="text-2xl font-bold mb-4">Step 2: Setup</h3>
                  <p className="text-muted-foreground">
                    Configure your preferences
                  </p>
                </div>
              </Step>

              <Step>
                <div className="text-center py-8">
                  <h3 className="text-2xl font-bold mb-4">Step 3: Launch</h3>
                  <p className="text-muted-foreground mb-6">
                    You're ready to go!
                  </p>
                  <Link href={buttonLink}>
                    <Button size="lg" variant={buttonVariant} className="gap-2">
                      {buttonText}
                      <ArrowRight className="h-5 w-5" />
                    </Button>
                  </Link>
                </div>
              </Step>
            </Stepper>
          </div>
        </ElectricBorder>
      </div>
    </section>
  );
}
```

---

## CTA-32: Elastic Slider with Star Border

**Layout**: Interactive slider dengan animated border  
**Animation**: ElasticSlider, StarBorder **Style**: Premium interactive control

```tsx
// Cta32.tsx
'use client';

import { useState } from 'react';
import Link from 'next/link';
import { ArrowRight } from 'lucide-react';
import { Button } from '@/components/ui/button';
import ElasticSlider from '@/components/ui/ElasticSlider';
import StarBorder from '@/components/ui/StarBorder';
import DecryptedText from '@/components/ui/DecryptedText';

/**
 * Cta32 Props - Mapped from Data Contract (LANDING-DATA-CONTRACT.md)
 *
 * @prop title - ctaTitle: CTA heading
 * @prop subtitle - ctaSubtitle: CTA description
 * @prop buttonText - ctaButtonText: Button label
 * @prop buttonLink - ctaButtonLink: Button destination URL
 * @prop buttonVariant - ctaButtonStyle: 'default' | 'secondary' | 'outline'
 */
interface Cta32Props {
  title: string;
  subtitle?: string;
  buttonText: string;
  buttonLink: string;
  buttonVariant: 'default' | 'secondary' | 'outline';
}

/**
 * CTA Block: cta32
 * Design: Interactive Slider Control
 */
export function Cta32({
  title,
  subtitle,
  buttonText,
  buttonLink,
  buttonVariant,
}: Cta32Props) {
  const [sliderValue, setSliderValue] = useState(50);

  return (
    <section className="py-20 my-8">
      <div className="max-w-4xl mx-auto px-4">
        <StarBorder
          as="div"
          color="primary"
          speed="5s"
          className="p-8 md:p-12 rounded-3xl bg-card"
        >
          <div className="text-center">
            {/* Decrypted Text Title */}
            <DecryptedText
              text={title}
              speed={60}
              maxIterations={10}
              animateOn="view"
              revealDirection="start"
              className="text-4xl md:text-5xl lg:text-6xl font-extrabold mb-6 block"
            />

            {subtitle && (
              <p className="text-lg md:text-xl text-muted-foreground mb-8 max-w-2xl mx-auto">
                {subtitle}
              </p>
            )}

            {/* Interactive Slider */}
            <div className="max-w-md mx-auto mb-8">
              <div className="mb-4">
                <p className="text-sm text-muted-foreground mb-2">
                  Adjust your preference:{' '}
                  <span className="font-bold text-foreground">
                    {sliderValue}%
                  </span>
                </p>
                <ElasticSlider
                  startingValue={0}
                  defaultValue={sliderValue}
                  maxValue={100}
                  isStepped={false}
                  stepSize={1}
                  leftIcon={<span>🔉</span>}
                  rightIcon={<span>🔊</span>}
                />
              </div>
            </div>

            <Link href={buttonLink}>
              <Button
                size="lg"
                variant={buttonVariant}
                className="gap-2 h-14 px-8 text-lg"
              >
                {buttonText}
                <ArrowRight className="h-5 w-5" />
              </Button>
            </Link>
          </div>
        </StarBorder>
      </div>
    </section>
  );
}
```

---

## DEPENDENCIES & INSTALLATION

```bash
# Text Animation Components
pnpm dlx shadcn@latest add @react-bits/ASCIIText-TS-CSS
pnpm dlx shadcn@latest add @react-bits/CircularText-TS-CSS
pnpm dlx shadcn@latest add @react-bits/CurvedLoop-TS-CSS
pnpm dlx shadcn@latest add @react-bits/ShinyText-TS-CSS
pnpm dlx shadcn@latest add @react-bits/GradientText-TS-CSS
pnpm dlx shadcn@latest add @react-bits/SplitText-TS-CSS
pnpm dlx shadcn@latest add @react-bits/DecryptedText-TS-CSS

# UI Components
pnpm dlx shadcn@latest add @react-bits/AnimatedList-TS-CSS
pnpm dlx shadcn@latest add @react-bits/Stepper-TS-CSS
pnpm dlx shadcn@latest add @react-bits/ElasticSlider-TS-CSS

# Interactive & Visual
pnpm dlx shadcn@latest add @react-bits/ClickSpark-TS-CSS
pnpm dlx shadcn@latest add @react-bits/Magnet-TS-CSS
pnpm dlx shadcn@latest add @react-bits/ElectricBorder-TS-CSS
pnpm dlx shadcn@latest add @react-bits/StarBorder-TS-CSS
```

---

## SUMMARY TABLE

| Component  | Primary Animation | Secondary Feature | Interactive    | Best For         |
| ---------- | ----------------- | ----------------- | -------------- | ---------------- |
| **CTA-28** | ASCIIText         | CircularText      | ClickSpark     | Tech/Retro       |
| **CTA-29** | CurvedLoop        | ShinyText         | Magnet         | Modern/Flow      |
| **CTA-30** | GradientText      | AnimatedList      | List Selection | Feature Showcase |
| **CTA-31** | SplitText         | Stepper           | Multi-step     | Onboarding       |
| **CTA-32** | DecryptedText     | ElasticSlider     | Slider Control | Interactive      |

---

**READY FOR PRODUCTION!** 🚀✨
