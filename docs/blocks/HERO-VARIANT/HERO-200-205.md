# HERO VARIANTS 42-46 - DIVERSE LAYOUTS

## HERO 42 - CENTERED GALAXY (Full Width Center)

```tsx
'use client';

import Link from 'next/link';
import { motion } from 'framer-motion';
import { Sparkles } from 'lucide-react';
import { InteractiveHoverButton } from '@/components/ui/interactive-hover-button';
import { OptimizedImage } from '@/components/ui/optimized-image';
import { Badge } from '@/components/ui/badge';
import Galaxy from '@/components/ui/galaxy';
import SplitText from '@/components/ui/split-text';

interface Hero42Props {
  title: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
  showCta?: boolean;
  backgroundImage?: string;
  logo?: string;
  storeName?: string;
}

export function Hero42({
  title,
  subtitle,
  ctaText,
  ctaLink = '/products',
  showCta = true,
  backgroundImage,
  logo,
  storeName,
}: Hero42Props) {
  return (
    <section className="relative min-h-screen overflow-hidden bg-black text-white flex items-center justify-center">
      <div className="absolute inset-0 z-0">
        <Galaxy
          mouseRepulsion
          mouseInteraction
          density={1.2}
          glowIntensity={0.4}
          hueShift={140}
          twinkleIntensity={0.3}
          rotationSpeed={0.1}
        />
      </div>

      <div className="container relative z-10 px-4 sm:px-6 lg:px-8">
        <div className="max-w-4xl mx-auto text-center space-y-6 sm:space-y-8 md:space-y-10">
          {logo && (
            <motion.div
              initial={{ opacity: 0, scale: 0.5 }}
              animate={{ opacity: 1, scale: 1 }}
              transition={{ duration: 0.6, type: "spring" }}
              className="flex justify-center"
            >
              <div className="relative h-20 w-20 sm:h-24 sm:w-24 md:h-28 md:w-28 rounded-full overflow-hidden border-4 border-white/30 shadow-2xl shadow-blue-500/50">
                <OptimizedImage
                  src={logo}
                  alt={storeName || title}
                  fill
                  className="object-cover"
                />
              </div>
            </motion.div>
          )}

          <motion.div
            initial={{ opacity: 0, y: -20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.2, duration: 0.6 }}
          >
            <Badge variant="secondary" className="px-4 py-2 text-base md:text-lg bg-white/10 text-white border-white/20 backdrop-blur-sm">
              <Sparkles className="h-4 w-4 mr-2" />
              {storeName || 'Featured'}
            </Badge>
          </motion.div>

          <div className="space-y-4">
            <SplitText
              text={title}
              className="text-4xl sm:text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-bold text-white"
              delay={50}
              duration={1}
              from={{ opacity: 0, y: 50 }}
              to={{ opacity: 1, y: 0 }}
            />

            {subtitle && (
              <motion.p
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                transition={{ delay: 0.8, duration: 0.6 }}
                className="text-lg sm:text-xl md:text-2xl text-white/80 max-w-2xl mx-auto leading-relaxed"
              >
                {subtitle}
              </motion.p>
            )}
          </div>

          {showCta && ctaText && (
            <motion.div
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: 1, duration: 0.6 }}
              className="pt-4"
            >
              <Link href={ctaLink}>
                <InteractiveHoverButton className="min-w-[250px] text-lg px-10 py-6">
                  {ctaText}
                </InteractiveHoverButton>
              </Link>
            </motion.div>
          )}
        </div>
      </div>
    </section>
  );
}
```

**Summary:** Centered full-width layout, Galaxy background, SplitText animation, rounded logo, centered alignment

---

## HERO 43 - AURORA ASYMMETRIC (Left Content + Right Floating Image)

```tsx
'use client';

import Link from 'next/link';
import { motion } from 'framer-motion';
import { Sparkles } from 'lucide-react';
import { InteractiveHoverButton } from '@/components/ui/interactive-hover-button';
import { OptimizedImage } from '@/components/ui/optimized-image';
import { Badge } from '@/components/ui/badge';
import Aurora from '@/components/ui/aurora';
import BlurText from '@/components/ui/blur-text';
import Magnet from '@/components/ui/magnet';

interface Hero43Props {
  title: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
  showCta?: boolean;
  backgroundImage?: string;
  logo?: string;
  storeName?: string;
}

export function Hero43({
  title,
  subtitle,
  ctaText,
  ctaLink = '/products',
  showCta = true,
  backgroundImage,
  logo,
  storeName,
}: Hero43Props) {
  return (
    <section className="relative min-h-screen overflow-hidden bg-black text-white">
      <div className="absolute inset-0 z-0">
        <Aurora
          colorStops={["#7cff67", "#B19EEF", "#5227FF"]}
          blend={0.5}
          amplitude={1.0}
          speed={1}
        />
      </div>

      <div className="container relative z-10 px-4 sm:px-6 lg:px-8 py-20 md:py-24 lg:py-32">
        <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-16 items-center">
          <div className="lg:col-span-7 space-y-6 md:space-y-8">
            {logo && (
              <motion.div
                initial={{ opacity: 0, x: -30 }}
                animate={{ opacity: 1, x: 0 }}
                transition={{ duration: 0.6 }}
              >
                <div className="relative h-16 w-16 md:h-20 md:w-20 rounded-xl overflow-hidden border-2 border-purple-400/40 shadow-2xl shadow-purple-500/30">
                  <OptimizedImage
                    src={logo}
                    alt={storeName || title}
                    fill
                    className="object-cover"
                  />
                </div>
              </motion.div>
            )}

            <motion.div
              initial={{ opacity: 0, x: -30 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ delay: 0.2, duration: 0.6 }}
            >
              <Badge variant="secondary" className="px-4 py-2 text-sm md:text-base bg-gradient-to-r from-purple-500/20 to-green-500/20 text-white border-purple-400/30">
                <Sparkles className="h-4 w-4 mr-2" />
                {storeName || 'Featured'}
              </Badge>
            </motion.div>

            <div className="space-y-4">
              <motion.h1
                initial={{ opacity: 0, x: -30 }}
                animate={{ opacity: 1, x: 0 }}
                transition={{ delay: 0.3, duration: 0.6 }}
                className="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-white leading-tight"
              >
                {title}
              </motion.h1>

              {subtitle && (
                <BlurText
                  text={subtitle}
                  delay={150}
                  animateBy="words"
                  direction="top"
                  className="text-lg md:text-xl text-purple-100/80 leading-relaxed"
                />
              )}
            </div>

            {showCta && ctaText && (
              <motion.div
                initial={{ opacity: 0, x: -30 }}
                animate={{ opacity: 1, x: 0 }}
                transition={{ delay: 0.6, duration: 0.6 }}
                className="pt-4"
              >
                <Magnet magnetStrength={40}>
                  <Link href={ctaLink}>
                    <InteractiveHoverButton className="min-w-[220px] text-base md:text-lg px-8 py-5">
                      {ctaText}
                    </InteractiveHoverButton>
                  </Link>
                </Magnet>
              </motion.div>
            )}
          </div>

          <motion.div
            initial={{ opacity: 0, x: 50, rotate: -5 }}
            animate={{ opacity: 1, x: 0, rotate: 0 }}
            transition={{ duration: 0.8, delay: 0.4 }}
            className="lg:col-span-5"
          >
            <div className="relative h-[300px] sm:h-[400px] md:h-[500px] lg:h-[600px] rounded-3xl overflow-hidden shadow-2xl shadow-purple-500/40 transform lg:rotate-3 hover:rotate-0 transition-transform duration-500">
              {backgroundImage ? (
                <OptimizedImage
                  src={backgroundImage}
                  alt={storeName || title}
                  fill
                  priority
                  className="object-cover"
                />
              ) : (
                <div className="absolute inset-0 bg-gradient-to-br from-purple-600/30 via-green-500/20 to-black flex items-center justify-center">
                  <span className="text-8xl opacity-20">🌈</span>
                </div>
              )}
            </div>
          </motion.div>
        </div>
      </div>
    </section>
  );
}
```

**Summary:** Asymmetric 7-5 grid, Aurora background, BlurText subtitle, Magnet CTA, rotated floating image

---

## HERO 44 - GRID SCAN VERTICAL STACK (Image Top + Content Bottom)

```tsx
'use client';

import Link from 'next/link';
import { motion } from 'framer-motion';
import { Sparkles } from 'lucide-react';
import { InteractiveHoverButton } from '@/components/ui/interactive-hover-button';
import { OptimizedImage } from '@/components/ui/optimized-image';
import { Badge } from '@/components/ui/badge';
import GridScan from '@/components/ui/grid-scan';
import DecryptedText from '@/components/ui/decrypted-text';

interface Hero44Props {
  title: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
  showCta?: boolean;
  backgroundImage?: string;
  logo?: string;
  storeName?: string;
}

export function Hero44({
  title,
  subtitle,
  ctaText,
  ctaLink = '/products',
  showCta = true,
  backgroundImage,
  logo,
  storeName,
}: Hero44Props) {
  return (
    <section className="relative min-h-screen overflow-hidden bg-black text-white">
      <div className="absolute inset-0 z-0">
        <GridScan
          gridColor="#00ff88"
          gridSize={50}
          gridOpacity={0.15}
          scanSpeed={2}
          scanGradient={["rgba(0,255,136,0)", "rgba(0,255,136,0.8)", "rgba(0,255,136,0)"]}
          scanHeight={200}
          glowSize={150}
        />
      </div>

      <div className="container relative z-10 px-4 sm:px-6 lg:px-8 py-12 md:py-16 lg:py-20">
        <div className="max-w-5xl mx-auto space-y-8 md:space-y-12">
          <motion.div
            initial={{ opacity: 0, y: -30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            className="relative h-[350px] sm:h-[450px] md:h-[550px] rounded-2xl overflow-hidden shadow-2xl shadow-emerald-500/30 border border-emerald-400/20"
          >
            {backgroundImage ? (
              <OptimizedImage
                src={backgroundImage}
                alt={storeName || title}
                fill
                priority
                className="object-cover"
              />
            ) : (
              <div className="absolute inset-0 bg-gradient-to-br from-emerald-600/20 via-teal-500/10 to-black flex items-center justify-center">
                <span className="text-9xl opacity-20">⚡</span>
              </div>
            )}
            <div className="absolute inset-x-0 bottom-0 h-32 bg-gradient-to-t from-black to-transparent" />
          </motion.div>

          <div className="text-center space-y-6 md:space-y-8">
            <div className="flex items-center justify-center gap-4">
              {logo && (
                <motion.div
                  initial={{ opacity: 0, scale: 0.8 }}
                  animate={{ opacity: 1, scale: 1 }}
                  transition={{ delay: 0.3, duration: 0.5 }}
                >
                  <div className="relative h-14 w-14 md:h-16 md:w-16 rounded-xl overflow-hidden border-2 border-emerald-400/30 shadow-xl shadow-emerald-500/20">
                    <OptimizedImage
                      src={logo}
                      alt={storeName || title}
                      fill
                      className="object-cover"
                    />
                  </div>
                </motion.div>
              )}

              <motion.div
                initial={{ opacity: 0, scale: 0.8 }}
                animate={{ opacity: 1, scale: 1 }}
                transition={{ delay: 0.4, duration: 0.5 }}
              >
                <Badge variant="secondary" className="px-3 py-1.5 text-sm md:text-base bg-emerald-500/20 text-emerald-300 border-emerald-400/30">
                  <Sparkles className="h-4 w-4 mr-2" />
                  {storeName || 'Featured'}
                </Badge>
              </motion.div>
            </div>

            <DecryptedText
              text={title}
              speed={50}
              className="text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl font-bold text-white drop-shadow-[0_0_20px_rgba(0,255,136,0.3)]"
              animateOn="view"
            />

            {subtitle && (
              <motion.p
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                transition={{ delay: 0.8, duration: 0.6 }}
                className="text-base sm:text-lg md:text-xl text-emerald-100/80 max-w-2xl mx-auto leading-relaxed"
              >
                {subtitle}
              </motion.p>
            )}

            {showCta && ctaText && (
              <motion.div
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: 1, duration: 0.6 }}
                className="pt-4"
              >
                <Link href={ctaLink}>
                  <InteractiveHoverButton className="min-w-[240px] text-base md:text-lg px-8 py-5">
                    {ctaText}
                  </InteractiveHoverButton>
                </Link>
              </motion.div>
            )}
          </div>
        </div>
      </div>
    </section>
  );
}
```

**Summary:** Vertical stack (image top, content bottom), Grid Scan background, DecryptedText title, centered layout

---

## HERO 45 - PARTICLES DIAGONAL (Content Diagonal + Floating Image)

```tsx
'use client';

import Link from 'next/link';
import { motion } from 'framer-motion';
import { Sparkles } from 'lucide-react';
import { InteractiveHoverButton } from '@/components/ui/interactive-hover-button';
import { OptimizedImage } from '@/components/ui/optimized-image';
import { Badge } from '@/components/ui/badge';
import Particles from '@/components/ui/particles';
import GradientText from '@/components/ui/gradient-text';

interface Hero45Props {
  title: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
  showCta?: boolean;
  backgroundImage?: string;
  logo?: string;
  storeName?: string;
}

export function Hero45({
  title,
  subtitle,
  ctaText,
  ctaLink = '/products',
  showCta = true,
  backgroundImage,
  logo,
  storeName,
}: Hero45Props) {
  return (
    <section className="relative min-h-screen overflow-hidden bg-black text-white">
      <div className="absolute inset-0 z-0">
        <Particles
          particleColors={["#ffffff", "#B19EEF", "#5227FF"]}
          particleCount={150}
          particleSpread={10}
          speed={0.1}
          particleBaseSize={100}
          moveParticlesOnHover
        />
      </div>

      <div className="container relative z-10 px-4 sm:px-6 lg:px-8 min-h-screen flex items-center">
        <div className="grid grid-cols-1 md:grid-cols-2 gap-12 lg:gap-20 items-center w-full">
          <div className="space-y-6 md:space-y-8 order-2 md:order-1">
            <div className="flex items-center gap-4">
              {logo && (
                <motion.div
                  initial={{ opacity: 0, rotate: -45 }}
                  animate={{ opacity: 1, rotate: 0 }}
                  transition={{ duration: 0.6, type: "spring" }}
                >
                  <div className="relative h-16 w-16 md:h-20 md:w-20 rounded-2xl overflow-hidden border-2 border-white/20 shadow-2xl shadow-purple-500/30 transform hover:rotate-12 transition-transform">
                    <OptimizedImage
                      src={logo}
                      alt={storeName || title}
                      fill
                      className="object-cover"
                    />
                  </div>
                </motion.div>
              )}

              <motion.div
                initial={{ opacity: 0, x: -20 }}
                animate={{ opacity: 1, x: 0 }}
                transition={{ delay: 0.2, duration: 0.6 }}
              >
                <Badge variant="secondary" className="px-3 py-2 text-sm md:text-base bg-white/10 text-white border-white/20 backdrop-blur-sm">
                  <Sparkles className="h-4 w-4 mr-2" />
                  {storeName || 'Featured'}
                </Badge>
              </motion.div>
            </div>

            <motion.div
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: 0.3, duration: 0.6 }}
            >
              <GradientText
                colors={["#ffffff", "#B19EEF", "#5227FF"]}
                animationSpeed={8}
                className="text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl font-bold leading-tight"
              >
                {title}
              </GradientText>
            </motion.div>

            {subtitle && (
              <motion.p
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: 0.5, duration: 0.6 }}
                className="text-base sm:text-lg md:text-xl text-white/70 leading-relaxed"
              >
                {subtitle}
              </motion.p>
            )}

            {showCta && ctaText && (
              <motion.div
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: 0.7, duration: 0.6 }}
                className="pt-4"
              >
                <Link href={ctaLink}>
                  <InteractiveHoverButton className="min-w-[220px] text-base md:text-lg px-8 py-5">
                    {ctaText}
                  </InteractiveHoverButton>
                </Link>
              </motion.div>
            )}
          </div>

          <motion.div
            initial={{ opacity: 0, scale: 0.8, rotate: 10 }}
            animate={{ opacity: 1, scale: 1, rotate: -3 }}
            transition={{ duration: 0.8, delay: 0.4 }}
            className="order-1 md:order-2"
          >
            <div className="relative h-[280px] sm:h-[350px] md:h-[450px] lg:h-[550px] rounded-3xl overflow-hidden shadow-2xl shadow-purple-500/30 transform hover:scale-105 hover:rotate-0 transition-all duration-500">
              {backgroundImage ? (
                <OptimizedImage
                  src={backgroundImage}
                  alt={storeName || title}
                  fill
                  priority
                  className="object-cover"
                />
              ) : (
                <div className="absolute inset-0 bg-gradient-to-br from-purple-600/20 via-blue-500/10 to-black flex items-center justify-center">
                  <span className="text-8xl opacity-20">✨</span>
                </div>
              )}
            </div>
          </motion.div>
        </div>
      </div>
    </section>
  );
}
```

**Summary:** Diagonal layout (50-50 grid), Particles background, GradientText title, rotated logo/image hover effects

---

## HERO 46 - LIGHT PILLAR OVERLAP (Overlapping Content + Image)

```tsx
'use client';

import Link from 'next/link';
import { motion } from 'framer-motion';
import { Sparkles } from 'lucide-react';
import { InteractiveHoverButton } from '@/components/ui/interactive-hover-button';
import { OptimizedImage } from '@/components/ui/optimized-image';
import { Badge } from '@/components/ui/badge';
import LightPillar from '@/components/ui/light-pillar';
import CircularText from '@/components/ui/circular-text';

interface Hero46Props {
  title: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
  showCta?: boolean;
  backgroundImage?: string;
  logo?: string;
  storeName?: string;
}

export function Hero46({
  title,
  subtitle,
  ctaText,
  ctaLink = '/products',
  showCta = true,
  backgroundImage,
  logo,
  storeName,
}: Hero46Props) {
  return (
    <section className="relative min-h-screen overflow-hidden bg-black text-white">
      <div className="absolute inset-0 z-0">
        <LightPillar
          topColor="#5227FF"
          bottomColor="#FF9FFC"
          intensity={1}
          rotationSpeed={0.3}
          glowAmount={0.002}
          pillarWidth={3}
          pillarHeight={0.4}
          mixBlendMode="screen"
        />
      </div>

      <div className="container relative z-10 px-4 sm:px-6 lg:px-8 min-h-screen flex items-center py-20">
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-0 items-center w-full">
          <motion.div
            initial={{ opacity: 0, x: -50 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.8 }}
            className="space-y-6 md:space-y-8 lg:pr-12 z-20"
          >
            {logo && (
              <div className="relative w-fit">
                <motion.div
                  initial={{ opacity: 0, scale: 0 }}
                  animate={{ opacity: 1, scale: 1 }}
                  transition={{ delay: 0.2, duration: 0.6, type: "spring" }}
                >
                  <div className="relative h-20 w-20 md:h-24 md:w-24">
                    <CircularText
                      text={storeName ? `${storeName} ✦ ` : 'FEATURED ✦ '}
                      spinDuration={20}
                      className="text-xs text-pink-300"
                    />
                    <div className="absolute inset-2 rounded-full overflow-hidden border-2 border-pink-400/30 shadow-2xl shadow-pink-500/40">
                      <OptimizedImage
                        src={logo}
                        alt={storeName || title}
                        fill
                        className="object-cover"
                      />
                    </div>
                  </div>
                </motion.div>
              </div>
            )}

            <motion.div
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: 0.3, duration: 0.6 }}
            >
              <Badge variant="secondary" className="px-4 py-2 text-sm md:text-base bg-gradient-to-r from-purple-500/20 to-pink-500/20 text-white border-pink-400/30">
                <Sparkles className="h-4 w-4 mr-2" />
                {storeName || 'Featured'}
              </Badge>
            </motion.div>

            <motion.h1
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: 0.4, duration: 0.6 }}
              className="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-white leading-tight drop-shadow-[0_0_30px_rgba(255,159,252,0.4)]"
            >
              {title}
            </motion.h1>

            {subtitle && (
              <motion.p
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                transition={{ delay: 0.6, duration: 0.6 }}
                className="text-base sm:text-lg md:text-xl text-pink-100/80 leading-relaxed"
              >
                {subtitle}
              </motion.p>
            )}

            {showCta && ctaText && (
              <motion.div
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: 0.8, duration: 0.6 }}
                className="pt-4"
              >
                <Link href={ctaLink}>
                  <InteractiveHoverButton className="min-w-[220px] text-base md:text-lg px-8 py-5">
                    {ctaText}
                  </InteractiveHoverButton>
                </Link>
              </motion.div>
            )}
          </motion.div>

          <motion.div
            initial={{ opacity: 0, x: 50 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.8, delay: 0.3 }}
            className="relative lg:-ml-32 z-10"
          >
            <div className="relative h-[300px] sm:h-[400px] md:h-[500px] lg:h-[600px] rounded-3xl overflow-hidden shadow-2xl shadow-pink-500/40 border border-pink-400/20">
              {backgroundImage ? (
                <OptimizedImage
                  src={backgroundImage}
                  alt={storeName || title}
                  fill
                  priority
                  className="object-cover hover:scale-110 transition-transform duration-700"
                />
              ) : (
                <div className="absolute inset-0 bg-gradient-to-br from-purple-600/20 via-pink-500/20 to-black flex items-center justify-center">
                  <span className="text-9xl opacity-20">💫</span>
                </div>
              )}
              <div className="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent" />
            </div>
          </motion.div>
        </div>
      </div>
    </section>
  );
}
```

**Summary:** Overlapping layout (content overlaps image), Light Pillar background, CircularText around logo, pink/purple gradient

---

## INSTALL DEPENDENCIES

```bash
# Hero 42
pnpm dlx shadcn@latest add @react-bits/Galaxy-TS-CSS
pnpm dlx shadcn@latest add @react-bits/SplitText-TS-CSS

# Hero 43
pnpm dlx shadcn@latest add @react-bits/Aurora-TS-CSS
pnpm dlx shadcn@latest add @react-bits/BlurText-TS-CSS
pnpm dlx shadcn@latest add @react-bits/Magnet-TS-CSS

# Hero 44
pnpm dlx shadcn@latest add @react-bits/GridScan-TS-CSS
pnpm dlx shadcn@latest add @react-bits/DecryptedText-TS-CSS

# Hero 45
pnpm dlx shadcn@latest add @react-bits/Particles-TS-CSS
pnpm dlx shadcn@latest add @react-bits/GradientText-TS-CSS

# Hero 46
pnpm dlx shadcn@latest add @react-bits/LightPillar-TS-CSS
pnpm dlx shadcn@latest add @react-bits/CircularText-TS-CSS

# All heroes
pnpm add framer-motion lucide-react gsap
```

---

## LAYOUT SUMMARY

| Hero | Layout Type | Background | Text Effects | Interactive | Special Features |
|------|-------------|------------|--------------|-------------|------------------|
| 42 | Centered Full Width | Galaxy | SplitText | - | Rounded logo, centered |
| 43 | Asymmetric 7-5 Grid | Aurora | BlurText | Magnet CTA | Rotated image, diagonal |
| 44 | Vertical Stack | Grid Scan | DecryptedText | - | Image top, content bottom |
| 45 | Diagonal 50-50 | Particles | GradientText | Hover rotate | Rotated elements |
| 46 | Overlapping | Light Pillar | CircularText | Image hover scale | Content overlaps image |

**All heroes:** Dark mode (bg-black), Same data contract, No hardcoded content, Unique layouts
