# HERO VARIANTS 52-56 - ADVANCED LAYOUTS

## HERO 52 - PIXEL SNOW DIAGONAL OFFSET (Offset Grid Layout)

```tsx
'use client';

import Link from 'next/link';
import { motion } from 'framer-motion';
import { Sparkles } from 'lucide-react';
import { InteractiveHoverButton } from '@/components/ui/interactive-hover-button';
import { OptimizedImage } from '@/components/ui/optimized-image';
import { Badge } from '@/components/ui/badge';
import PixelSnow from '@/components/ui/pixel-snow';
import ASCIIText from '@/components/ui/ascii-text';

interface Hero52Props {
  title: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
  showCta?: boolean;
  backgroundImage?: string;
  logo?: string;
  storeName?: string;
}

export function Hero52({
  title,
  subtitle,
  ctaText,
  ctaLink = '/products',
  showCta = true,
  backgroundImage,
  logo,
  storeName,
}: Hero52Props) {
  return (
    <section className="relative min-h-screen overflow-hidden bg-black text-white">
      <div className="absolute inset-0 z-0">
        <PixelSnow
          color="#ffffff"
          flakeSize={0.01}
          minFlakeSize={1.25}
          pixelResolution={200}
          speed={1.25}
          density={0.3}
          direction={125}
          brightness={1}
          variant="square"
        />
      </div>

      <div className="container relative z-10 px-4 sm:px-6 lg:px-8 min-h-screen flex items-center py-20">
        <div className="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 w-full items-center">
          <motion.div
            initial={{ opacity: 0, x: -50 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.8 }}
            className="space-y-6 md:space-y-8 md:pt-20"
          >
            <div className="flex items-center gap-4">
              {logo && (
                <div className="relative h-16 w-16 md:h-20 md:w-20 rounded-2xl overflow-hidden border-2 border-white/20 shadow-2xl">
                  <OptimizedImage
                    src={logo}
                    alt={storeName || title}
                    fill
                    className="object-cover"
                  />
                </div>
              )}

              <Badge variant="secondary" className="px-3 py-2 text-sm md:text-base bg-white/10 text-white border-white/20 backdrop-blur-sm">
                <Sparkles className="h-4 w-4 mr-2" />
                {storeName || 'Featured'}
              </Badge>
            </div>

            <h1 className="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-white leading-tight">
              {title}
            </h1>

            {subtitle && (
              <motion.p
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                transition={{ delay: 0.4, duration: 0.6 }}
                className="text-base sm:text-lg md:text-xl text-white/70 leading-relaxed"
              >
                {subtitle}
              </motion.p>
            )}

            {showCta && ctaText && (
              <motion.div
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: 0.6, duration: 0.6 }}
                className="pt-4"
              >
                <Link href={ctaLink}>
                  <InteractiveHoverButton className="min-w-[220px] text-base md:text-lg px-8 py-5">
                    {ctaText}
                  </InteractiveHoverButton>
                </Link>
              </motion.div>
            )}
          </motion.div>

          <motion.div
            initial={{ opacity: 0, x: 50 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.8, delay: 0.2 }}
            className="grid grid-cols-2 gap-4 md:gap-6"
          >
            <div className="space-y-4 md:space-y-6">
              <div className="relative h-40 md:h-52 rounded-2xl overflow-hidden shadow-xl border border-white/10">
                {backgroundImage ? (
                  <OptimizedImage
                    src={backgroundImage}
                    alt={`${storeName || title} 1`}
                    fill
                    className="object-cover hover:scale-110 transition-transform duration-500"
                  />
                ) : (
                  <div className="absolute inset-0 bg-gradient-to-br from-blue-600/20 via-cyan-500/10 to-black flex items-center justify-center">
                    <span className="text-5xl opacity-20">❄️</span>
                  </div>
                )}
              </div>

              <div className="relative h-32 md:h-40 rounded-2xl overflow-hidden shadow-xl border border-white/10">
                {backgroundImage ? (
                  <OptimizedImage
                    src={backgroundImage}
                    alt={`${storeName || title} 3`}
                    fill
                    className="object-cover hover:scale-110 transition-transform duration-500"
                  />
                ) : (
                  <div className="absolute inset-0 bg-gradient-to-br from-cyan-600/20 via-blue-500/10 to-black flex items-center justify-center">
                    <span className="text-4xl opacity-20">❄️</span>
                  </div>
                )}
              </div>
            </div>

            <div className="space-y-4 md:space-y-6 pt-12 md:pt-16">
              <div className="relative h-32 md:h-40 rounded-2xl overflow-hidden shadow-xl border border-white/10">
                {backgroundImage ? (
                  <OptimizedImage
                    src={backgroundImage}
                    alt={`${storeName || title} 2`}
                    fill
                    className="object-cover hover:scale-110 transition-transform duration-500"
                  />
                ) : (
                  <div className="absolute inset-0 bg-gradient-to-br from-blue-500/20 via-cyan-600/10 to-black flex items-center justify-center">
                    <span className="text-4xl opacity-20">❄️</span>
                  </div>
                )}
              </div>

              <div className="relative h-40 md:h-52 rounded-2xl overflow-hidden shadow-xl border border-white/10">
                {backgroundImage ? (
                  <OptimizedImage
                    src={backgroundImage}
                    alt={`${storeName || title} 4`}
                    fill
                    className="object-cover hover:scale-110 transition-transform duration-500"
                  />
                ) : (
                  <div className="absolute inset-0 bg-gradient-to-br from-cyan-500/20 via-blue-600/10 to-black flex items-center justify-center">
                    <span className="text-5xl opacity-20">❄️</span>
                  </div>
                )}
              </div>
            </div>
          </motion.div>
        </div>
      </div>
    </section>
  );
}
```

**Summary:** Diagonal offset grid, Pixel Snow background, masonry-style image grid right, content left offset

---

## HERO 53 - DITHER WAVE CAROUSEL (Horizontal Scroll Stack)

```tsx
'use client';

import Link from 'next/link';
import { motion } from 'framer-motion';
import { Sparkles } from 'lucide-react';
import { InteractiveHoverButton } from '@/components/ui/interactive-hover-button';
import { OptimizedImage } from '@/components/ui/optimized-image';
import { Badge } from '@/components/ui/badge';
import Dither from '@/components/ui/dither';
import FuzzyText from '@/components/ui/fuzzy-text';

interface Hero53Props {
  title: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
  showCta?: boolean;
  backgroundImage?: string;
  logo?: string;
  storeName?: string;
}

export function Hero53({
  title,
  subtitle,
  ctaText,
  ctaLink = '/products',
  showCta = true,
  backgroundImage,
  logo,
  storeName,
}: Hero53Props) {
  return (
    <section className="relative min-h-screen overflow-hidden bg-black text-white">
      <div className="absolute inset-0 z-0">
        <Dither
          waveColor={[0.5, 0.5, 0.5]}
          disableAnimation={false}
          enableMouseInteraction
          mouseRadius={0.3}
          colorNum={4}
          waveAmplitude={0.3}
          waveFrequency={3}
          waveSpeed={0.05}
        />
      </div>

      <div className="container relative z-10 px-4 sm:px-6 lg:px-8 py-16 md:py-20">
        <div className="max-w-7xl mx-auto space-y-12 md:space-y-16">
          <div className="max-w-3xl space-y-6 md:space-y-8">
            <div className="flex items-center gap-4 flex-wrap">
              {logo && (
                <motion.div
                  initial={{ opacity: 0, scale: 0.5 }}
                  animate={{ opacity: 1, scale: 1 }}
                  transition={{ duration: 0.6 }}
                >
                  <div className="relative h-16 w-16 md:h-20 md:w-20 rounded-xl overflow-hidden border-2 border-gray-400/30 shadow-2xl">
                    <OptimizedImage
                      src={logo}
                      alt={storeName || title}
                      fill
                      className="object-cover"
                    />
                  </div>
                </motion.div>
              )}

              <motion.div
                initial={{ opacity: 0, x: -20 }}
                animate={{ opacity: 1, x: 0 }}
                transition={{ delay: 0.2, duration: 0.6 }}
              >
                <Badge variant="secondary" className="px-3 py-2 text-sm md:text-base bg-gray-500/20 text-gray-200 border-gray-400/30">
                  <Sparkles className="h-4 w-4 mr-2" />
                  {storeName || 'Featured'}
                </Badge>
              </motion.div>
            </div>

            <motion.div
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ delay: 0.3, duration: 0.6 }}
            >
              <FuzzyText
                baseIntensity={0.2}
                hoverIntensity={0.5}
                enableHover
              >
                <h1 className="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-white leading-tight">
                  {title}
                </h1>
              </FuzzyText>
            </motion.div>

            {subtitle && (
              <motion.p
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                transition={{ delay: 0.5, duration: 0.6 }}
                className="text-base sm:text-lg md:text-xl text-gray-300/80 leading-relaxed"
              >
                {subtitle}
              </motion.p>
            )}

            {showCta && ctaText && (
              <motion.div
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: 0.7, duration: 0.6 }}
                className="pt-4"
              >
                <Link href={ctaLink}>
                  <InteractiveHoverButton className="min-w-[220px] text-base md:text-lg px-8 py-5">
                    {ctaText}
                  </InteractiveHoverButton>
                </Link>
              </motion.div>
            )}
          </div>

          <motion.div
            initial={{ opacity: 0, x: -50 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.8, delay: 0.4 }}
            className="overflow-x-auto scrollbar-hide"
          >
            <div className="flex gap-4 md:gap-6 pb-4 min-w-max">
              {[1, 2, 3, 4, 5].map((i) => (
                <div
                  key={i}
                  className="relative w-64 md:w-80 h-64 md:h-80 rounded-2xl overflow-hidden shadow-xl border border-gray-400/20 flex-shrink-0 group"
                >
                  {backgroundImage ? (
                    <OptimizedImage
                      src={backgroundImage}
                      alt={`${storeName || title} ${i}`}
                      fill
                      className="object-cover group-hover:scale-110 transition-transform duration-500"
                    />
                  ) : (
                    <div className="absolute inset-0 bg-gradient-to-br from-gray-600/20 via-gray-500/10 to-black flex items-center justify-center">
                      <span className="text-6xl opacity-20">{i}</span>
                    </div>
                  )}
                </div>
              ))}
            </div>
          </motion.div>
        </div>
      </div>
    </section>
  );
}
```

**Summary:** Horizontal scroll stack, Dither wave background, FuzzyText title, carousel-style images

---

## HERO 54 - FAULTY TERMINAL SPLIT VERTICAL (Top-Bottom Split)

```tsx
'use client';

import Link from 'next/link';
import { motion } from 'framer-motion';
import { Sparkles } from 'lucide-react';
import { InteractiveHoverButton } from '@/components/ui/interactive-hover-button';
import { OptimizedImage } from '@/components/ui/optimized-image';
import { Badge } from '@/components/ui/badge';
import FaultyTerminal from '@/components/ui/faulty-terminal';
import TrueFocus from '@/components/ui/true-focus';

interface Hero54Props {
  title: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
  showCta?: boolean;
  backgroundImage?: string;
  logo?: string;
  storeName?: string;
}

export function Hero54({
  title,
  subtitle,
  ctaText,
  ctaLink = '/products',
  showCta = true,
  backgroundImage,
  logo,
  storeName,
}: Hero54Props) {
  return (
    <section className="relative min-h-screen overflow-hidden bg-black text-white">
      <div className="absolute inset-0 z-0">
        <FaultyTerminal
          scale={1.5}
          gridMul={[2, 1]}
          digitSize={1.2}
          timeScale={0.5}
          scanlineIntensity={0.5}
          glitchAmount={1}
          flickerAmount={1}
          tint="#A7EF9E"
          mouseReact
          mouseStrength={0.5}
          brightness={0.6}
        />
      </div>

      <div className="container relative z-10 px-4 sm:px-6 lg:px-8 min-h-screen flex flex-col justify-between py-12 md:py-16">
        <motion.div
          initial={{ opacity: 0, y: -30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8 }}
          className="flex-1 flex items-center justify-center"
        >
          <div className="max-w-4xl text-center space-y-8 md:space-y-10">
            <div className="flex items-center justify-center gap-4">
              {logo && (
                <div className="relative h-16 w-16 md:h-20 md:w-20 rounded-xl overflow-hidden border-2 border-green-400/30 shadow-2xl shadow-green-500/30">
                  <OptimizedImage
                    src={logo}
                    alt={storeName || title}
                    fill
                    className="object-cover"
                  />
                </div>
              )}

              <Badge variant="secondary" className="px-4 py-2 text-sm md:text-base bg-green-500/20 text-green-200 border-green-400/30">
                <Sparkles className="h-4 w-4 mr-2" />
                {storeName || 'Featured'}
              </Badge>
            </div>

            <TrueFocus
              sentence={title}
              blurAmount={5}
              borderColor="#A7EF9E"
              animationDuration={0.5}
              pauseBetweenAnimations={1}
            />

            {subtitle && (
              <motion.p
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                transition={{ delay: 0.6, duration: 0.6 }}
                className="text-base sm:text-lg md:text-xl text-green-100/80 max-w-2xl mx-auto leading-relaxed"
              >
                {subtitle}
              </motion.p>
            )}

            {showCta && ctaText && (
              <motion.div
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: 0.8, duration: 0.6 }}
                className="pt-4"
              >
                <Link href={ctaLink}>
                  <InteractiveHoverButton className="min-w-[240px] text-base md:text-lg px-10 py-6">
                    {ctaText}
                  </InteractiveHoverButton>
                </Link>
              </motion.div>
            )}
          </div>
        </motion.div>

        <motion.div
          initial={{ opacity: 0, y: 50 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8, delay: 0.4 }}
          className="max-w-5xl mx-auto w-full"
        >
          <div className="relative h-48 md:h-64 lg:h-80 rounded-2xl overflow-hidden shadow-2xl border border-green-400/20">
            {backgroundImage ? (
              <OptimizedImage
                src={backgroundImage}
                alt={storeName || title}
                fill
                priority
                className="object-cover"
              />
            ) : (
              <div className="absolute inset-0 bg-gradient-to-br from-green-600/20 via-emerald-500/10 to-black flex items-center justify-center">
                <span className="text-9xl opacity-20">💻</span>
              </div>
            )}
            <div className="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent" />
          </div>
        </motion.div>
      </div>
    </section>
  );
}
```

**Summary:** Top-bottom split, Faulty Terminal background, TrueFocus title, centered top, image bottom

---

## HERO 55 - LIGHTNING CORNER HERO (Corner Content + Full Image)

```tsx
'use client';

import Link from 'next/link';
import { motion } from 'framer-motion';
import { Sparkles } from 'lucide-react';
import { InteractiveHoverButton } from '@/components/ui/interactive-hover-button';
import { OptimizedImage } from '@/components/ui/optimized-image';
import { Badge } from '@/components/ui/badge';
import Lightning from '@/components/ui/lightning';
import ElectricBorder from '@/components/ui/electric-border';

interface Hero55Props {
  title: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
  showCta?: boolean;
  backgroundImage?: string;
  logo?: string;
  storeName?: string;
}

export function Hero55({
  title,
  subtitle,
  ctaText,
  ctaLink = '/products',
  showCta = true,
  backgroundImage,
  logo,
  storeName,
}: Hero55Props) {
  return (
    <section className="relative min-h-screen overflow-hidden bg-black text-white">
      <div className="absolute inset-0 z-0">
        <Lightning
          hue={260}
          xOffset={0}
          speed={1}
          intensity={1}
          size={1}
        />
      </div>

      <div className="container relative z-10 px-4 sm:px-6 lg:px-8 min-h-screen py-12 md:py-16">
        <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 md:gap-12 min-h-[calc(100vh-6rem)]">
          <motion.div
            initial={{ opacity: 0, x: -50 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.8 }}
            className="lg:col-span-5 flex flex-col justify-center space-y-6 md:space-y-8"
          >
            <div className="flex items-center gap-4">
              {logo && (
                <div className="relative h-16 w-16 md:h-20 md:w-20 rounded-2xl overflow-hidden border-2 border-purple-400/30 shadow-2xl shadow-purple-500/40">
                  <OptimizedImage
                    src={logo}
                    alt={storeName || title}
                    fill
                    className="object-cover"
                  />
                </div>
              )}

              <Badge variant="secondary" className="px-3 py-2 text-sm md:text-base bg-purple-500/20 text-purple-200 border-purple-400/30">
                <Sparkles className="h-4 w-4 mr-2" />
                {storeName || 'Featured'}
              </Badge>
            </div>

            <h1 className="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-white leading-tight drop-shadow-[0_0_30px_rgba(139,92,246,0.5)]">
              {title}
            </h1>

            {subtitle && (
              <motion.p
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                transition={{ delay: 0.4, duration: 0.6 }}
                className="text-base sm:text-lg md:text-xl text-purple-100/80 leading-relaxed"
              >
                {subtitle}
              </motion.p>
            )}

            {showCta && ctaText && (
              <motion.div
                initial={{ opacity: 0, y: 20 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ delay: 0.6, duration: 0.6 }}
                className="pt-4"
              >
                <ElectricBorder
                  color="#8b5cf6"
                  speed={1}
                  chaos={0.12}
                  thickness={2}
                  style={{ borderRadius: 12 }}
                >
                  <Link href={ctaLink}>
                    <InteractiveHoverButton className="min-w-[220px] text-base md:text-lg px-8 py-5">
                      {ctaText}
                    </InteractiveHoverButton>
                  </Link>
                </ElectricBorder>
              </motion.div>
            )}
          </motion.div>

          <motion.div
            initial={{ opacity: 0, scale: 0.95 }}
            animate={{ opacity: 1, scale: 1 }}
            transition={{ duration: 0.8, delay: 0.3 }}
            className="lg:col-span-7"
          >
            <div className="relative h-[400px] lg:h-full rounded-3xl overflow-hidden shadow-2xl border border-purple-400/20">
              {backgroundImage ? (
                <OptimizedImage
                  src={backgroundImage}
                  alt={storeName || title}
                  fill
                  priority
                  className="object-cover"
                />
              ) : (
                <div className="absolute inset-0 bg-gradient-to-br from-purple-600/20 via-violet-500/20 to-black flex items-center justify-center">
                  <span className="text-9xl opacity-20">⚡</span>
                </div>
              )}
              <div className="absolute inset-0 bg-gradient-to-t from-black/40 via-transparent to-transparent" />
            </div>
          </motion.div>
        </div>
      </div>
    </section>
  );
}
```

**Summary:** Corner content layout (5-7 grid), Lightning background, ElectricBorder CTA, full-height image

---

## HERO 56 - BALLPIT PHYSICS CARDS (3D Card Stack)

```tsx
'use client';

import Link from 'next/link';
import { motion } from 'framer-motion';
import { Sparkles } from 'lucide-react';
import { InteractiveHoverButton } from '@/components/ui/interactive-hover-button';
import { OptimizedImage } from '@/components/ui/optimized-image';
import { Badge } from '@/components/ui/badge';
import Ballpit from '@/components/ui/ballpit';
import AnimatedContent from '@/components/ui/animated-content';

interface Hero56Props {
  title: string;
  subtitle?: string;
  ctaText?: string;
  ctaLink?: string;
  showCta?: boolean;
  backgroundImage?: string;
  logo?: string;
  storeName?: string;
}

export function Hero56({
  title,
  subtitle,
  ctaText,
  ctaLink = '/products',
  showCta = true,
  backgroundImage,
  logo,
  storeName,
}: Hero56Props) {
  return (
    <section className="relative min-h-screen overflow-hidden bg-black text-white">
      <div className="absolute inset-0 z-0 opacity-60">
        <Ballpit
          count={80}
          gravity={0.01}
          friction={0.9975}
          wallBounce={0.95}
          followCursor={false}
        />
      </div>

      <div className="container relative z-10 px-4 sm:px-6 lg:px-8 min-h-screen flex items-center py-20">
        <div className="max-w-6xl mx-auto w-full">
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16 items-center">
            <AnimatedContent
              distance={100}
              direction="vertical"
              duration={0.8}
              ease="power3.out"
              animateOpacity
            >
              <div className="space-y-6 md:space-y-8">
                <div className="flex items-center gap-4">
                  {logo && (
                    <div className="relative h-16 w-16 md:h-20 md:w-20 rounded-2xl overflow-hidden border-2 border-white/20 shadow-2xl backdrop-blur-sm">
                      <OptimizedImage
                        src={logo}
                        alt={storeName || title}
                        fill
                        className="object-cover"
                      />
                    </div>
                  )}

                  <Badge variant="secondary" className="px-3 py-2 text-sm md:text-base bg-white/10 text-white border-white/20 backdrop-blur-sm">
                    <Sparkles className="h-4 w-4 mr-2" />
                    {storeName || 'Featured'}
                  </Badge>
                </div>

                <h1 className="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold text-white leading-tight">
                  {title}
                </h1>

                {subtitle && (
                  <p className="text-base sm:text-lg md:text-xl text-white/70 leading-relaxed">
                    {subtitle}
                  </p>
                )}

                {showCta && ctaText && (
                  <div className="pt-4">
                    <Link href={ctaLink}>
                      <InteractiveHoverButton className="min-w-[220px] text-base md:text-lg px-8 py-5">
                        {ctaText}
                      </InteractiveHoverButton>
                    </Link>
                  </div>
                )}
              </div>
            </AnimatedContent>

            <div className="relative">
              <div className="grid grid-cols-2 gap-4 md:gap-6">
                {[1, 2, 3, 4].map((i, idx) => (
                  <motion.div
                    key={i}
                    initial={{ opacity: 0, y: 50, rotate: -10 }}
                    animate={{ 
                      opacity: 1, 
                      y: 0, 
                      rotate: idx % 2 === 0 ? 2 : -2 
                    }}
                    transition={{ 
                      duration: 0.6, 
                      delay: 0.2 + (idx * 0.1),
                      type: "spring"
                    }}
                    className="relative h-48 md:h-56 rounded-2xl overflow-hidden shadow-2xl border border-white/10 group transform hover:scale-105 hover:rotate-0 transition-all duration-300"
                  >
                    {backgroundImage ? (
                      <OptimizedImage
                        src={backgroundImage}
                        alt={`${storeName || title} ${i}`}
                        fill
                        className="object-cover"
                      />
                    ) : (
                      <div className="absolute inset-0 bg-gradient-to-br from-purple-600/30 via-pink-500/20 to-black flex items-center justify-center">
                        <span className="text-6xl opacity-20">{i}</span>
                      </div>
                    )}
                    <div className="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                  </motion.div>
                ))}
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
```

**Summary:** 3D card stack layout, Ballpit physics background, AnimatedContent wrapper, 2x2 image grid

---

## INSTALL DEPENDENCIES

```bash
# Hero 52
pnpm dlx shadcn@latest add @react-bits/PixelSnow-TS-CSS
pnpm dlx shadcn@latest add @react-bits/ASCIIText-TS-CSS

# Hero 53
pnpm dlx shadcn@latest add @react-bits/Dither-TS-CSS
pnpm dlx shadcn@latest add @react-bits/FuzzyText-TS-CSS

# Hero 54
pnpm dlx shadcn@latest add @react-bits/FaultyTerminal-TS-CSS
pnpm dlx shadcn@latest add @react-bits/TrueFocus-TS-CSS

# Hero 55
pnpm dlx shadcn@latest add @react-bits/Lightning-TS-CSS
pnpm dlx shadcn@latest add @react-bits/ElectricBorder-TS-CSS

# Hero 56
pnpm dlx shadcn@latest add @react-bits/Ballpit-TS-CSS
pnpm dlx shadcn@latest add @react-bits/AnimatedContent-TS-CSS

# All heroes
pnpm add framer-motion lucide-react gsap
```

---

## LAYOUT SUMMARY

| Hero | Layout Type | Background | Text Effects | Interactive | Special Features |
|------|-------------|------------|--------------|-------------|------------------|
| 52 | Diagonal Offset Grid | Pixel Snow | - | Image hover | Masonry offset grid |
| 53 | Horizontal Scroll | Dither Wave | FuzzyText | Mouse wave | Carousel stack |
| 54 | Top-Bottom Split | Faulty Terminal | TrueFocus | Mouse glitch | Centered top/bottom |
| 55 | Corner Content 5-7 | Lightning | - | ElectricBorder | Full-height image |
| 56 | 3D Card Stack | Ballpit Physics | AnimatedContent | Card hover | Physics balls |

**All heroes:** Dark mode (bg-black), Same data contract, Advanced layouts, Unique interactions
